<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="login.css">
        <title>Kirjaudu sisään</title>
    </head>
    <body>
        <div class="header">
            <h1>Forum-palvelu</h1>
        </div>
        <div class="form-container">
            <h2>Kirjaudu sisään</h2>
            <form action="login" method="post">
                <div class="form-group">
                    <label for="name">Käyttäjänimi</label>
                    <input type="text" id="name" name="username" placeholder="Syötä käyttäjänimi" required>
                </div>
                <div class="form-group">
                    <label for="password">Password</label>
                    <input type="password" name="password" id="password" placeholder="Salasana" required>
                </div>
                <% if (query && query.error) { %>
                    <p class="error-message"><%= query.error %></p>
                    <% } %>
                <button type="submit" class="submit-btn">Kirjaudu sisään</button>
            </form>
            <p>Et ole luonut tunnusta?</br><a href="/signup">Luo käyttäjä</a></p>
        </div>
        <% if (query && query.signup === 'success') { %>
            <script>
                alert('Rekisteröinti onnistui! Voit nyt kirjautua sisään.');
            </script>
            <% } %>
    </body>
</html>